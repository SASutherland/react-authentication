const Home = () => {
  return (
    <div className="bounds">
      <div className="grid-100">
        <h1>Welcome to Shawn's Authentication Test!</h1>
        <p>This React app allows users to sign up, log in, and change theme settings.</p>
        <p>Cookies are added to the browser to retrieve login information.</p>
        <p>(Remember to run <em>npm start</em> in both the client AND api directories.)</p>
      </div>
    </div>
  );
}

export default Home;
